<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Trees to plant"
        android:textAppearance="?attr/textAppearanceHeadline6"
        android:paddingBottom="16dp"/>

    <TextView
        android:id="@+id/text_no_trees"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="No trees ready to plant."
        android:textSize="18sp"
        android:visibility="gone"
        android:layout_gravity="center" />


    <!-- ExpandableListView for listing trees -->
    <ExpandableListView
        android:id="@+id/expandable_view_trees"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:divider="@android:color/darker_gray"
        android:dividerHeight="0.5dp"
        android:childDivider="@android:color/transparent"
        android:groupIndicator="@null"/>

    <!-- Buttons at the bottom -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:paddingTop="16dp">

        <Button
            android:id="@+id/button_plant_now"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Plant Now"
            android:layout_marginEnd="8dp"/>

        <Button
            android:id="@+id/button_delete"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Delete"/>
    </LinearLayout>

</LinearLayout>
